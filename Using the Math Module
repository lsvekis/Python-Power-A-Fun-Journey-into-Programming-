import math
# Calculate the square root of 16 and the cosine of 45 degrees (converted to radians)
sqrt_val = math.sqrt(16)
cos_val = math.cos(math.radians(45))
print("The square root of 16 is:", sqrt_val)
print("The cosine of 45° is:", cos_val)
